package runnable;

// 다른 사람이 만든 클래스여서
// 소스 수정할 수 없는 상태에 있다고 가정
public class Numbers {
	
	protected void list(int start, int end) {
		for (int cnt = start; cnt <= end; cnt++) {
			System.out.printf("(%d)", cnt);
		}
	}
	
}
